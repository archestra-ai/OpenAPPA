# Keep the runtime image build context to the crates its Dockerfile copies.
.git
target
**/target
website
# The appa runtime's build.rs stages this one doc into the plugin's
# build identity, so a release build of `appa` needs it in-context.
!website/content/docs/contracts.md
paper
docs
demo/*/world/**/.DS_Store
marketplace/batteries/**/__pycache__
marketplace/batteries/**/test_*.py
marketplace/batteries/test_cross_battery.py
