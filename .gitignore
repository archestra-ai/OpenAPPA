target/
.env
# Working runtime config and store (the install instruction copies the
# default example here; integrations/claude-code/README.md)
/appa.toml
/appa.db*
# LaTeX build artifacts (regenerate with `make`)
appa.pdf
*.aux
*.bbl
*.blg
*.log
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
*.toc
appa-arxiv.tar.gz
# Custom target dirs (CARGO_TARGET_DIR from tooling)
target-*/
.envrc
bench/taubench/runs/
bench/taubench/.tau2-bench/
bench/taubench/data/.embeddings_cache/
__pycache__/
*.py[cod]

# Local session state and scratch, never committed.
.DS_Store
/tmp/
/appa-mcp*
