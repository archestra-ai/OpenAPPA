target/
.env
# Working runtime config and store (the install instruction copies the
# default example here; marketplace/plugins/claude-code/README.md)
/appa.toml
/appa.db*
# Custom target dirs (CARGO_TARGET_DIR from tooling)
target-*/
.envrc
# Repository-root local data plus benchmark runs and caches
runs/
/data/
bench/**/runs/
bench/**/.venv/
bench/**/__pycache__/
bench/**/.pytest_cache/
bench/**/.ruff_cache/
# The Tau checkout the harness's setup script clones, and its embedding cache.
bench/taubench/.tau2-bench/
bench/taubench/data/.embeddings_cache/

.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# Local session state and scratch, never committed.
.DS_Store
/tmp/
/appa-mcp*

# Local Amp portal metadata.
.amp/portals/*

# Generated Amp-to-GCP external-account configuration. It contains no secret,
# but embeds this orb checkout's absolute path to its credential helper.
.amp/gcp-external-account.json
