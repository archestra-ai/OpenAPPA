{
  "name": "appa",
  "owner": {
    "name": "Archestra"
  },
  "plugins": [
    {
      "name": "appa-runtime",
      "source": "./plugin",
      "description": "Gates a Claude Code session's flows through the appa-runtime-v2 process: every prompt, tool call, tool result, and child return is checked before it proceeds. Hooks fail closed - the runtime process must be running first."
    }
  ]
}
