# Default OpenAPPA deployment for Claude Code.
# The runtime writes the included battery under `batteries/` on first start.

include = ["batteries/claude-code/appa.toml"]

[policy]
version = 1

[policy.deployment]
context_control = true

[externals]
timeout_ms = 5000
review_timeout_ms = 600000
max_body_bytes = 65536
