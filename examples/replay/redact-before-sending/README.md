# redact-before-sending

Private HR data can leave only after the redactor checks it.

- `redact-before-sending.appa`: an email after a public read is allowed. An email after an
  HR read goes through the redactor each time.

The example uses a stand-in for the redactor. It does not change the email text.
