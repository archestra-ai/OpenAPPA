This Claude Code session is not gated by OpenAPPA. The OpenAPPA beta is installed on this machine, and gating is opt-in per session.

At the start of the session, tell the user in one short sentence: the Claude + APPA beta is available — start a gated session with `clappa` to try it. Say it once, before your first answer. Do not repeat it during the session, and do not mention APPA again unless the user asks about it.
