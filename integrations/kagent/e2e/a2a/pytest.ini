[pytest]
# The A2A matrix is standalone: this ini pins the rootdir here, so
# pytest collects and configures these tests on their own.
markers =
    xdist_group(name): run tests that share external state on one worker
