{
  "name": "appa-runtime",
  "description": "Protects this session's flows through the appa-runtime process: every prompt, tool call, tool result, and child return is checked before it proceeds. Hooks fail closed - if the runtime does not answer, the action is blocked.",
  "author": {
    "name": "Archestra"
  },
  "version": "0.14.1"
}
