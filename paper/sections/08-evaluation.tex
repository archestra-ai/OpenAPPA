\section{Evaluation}\label{sec:evaluation}

\stub{Evaluation is designed but not yet run; this section states the protocol and metrics. Numbers TODO.}

\paragraph{Primary question.}
Does \texttt{fork}-before-read preserve the security of label-based enforcement while eliminating the post-read utility loss caused by tainting a long-lived planner context? The evaluation must measure this directly; a generic defended-versus-undefended benchmark does not isolate the paper's core contribution~\cite{debenedettiAgentDojoDynamicEnvironment2024}.

\paragraph{Paired trajectory benchmark.}
We will construct AgentDojo episodes~\cite{debenedettiAgentDojoDynamicEnvironment2024} in which an agent:
\begin{enumerate}
  \item performs a task requiring inspection of restrictive or attacker-controlled data, and
  \item subsequently executes an independent benign action whose sink would be disabled by the descended label.
\end{enumerate}
Task variants will cover a first task requiring no return value, a schema-bounded derivative, and a return value that cannot be safely derived. We will report second-task completion rates, strict descents of the \emph{parent} label, preservation of the parent's ruling-free release frontier, and branch outcomes (\texttt{abandon}, bounded return, or rejected vs. accepted raw \texttt{merge}).

\paragraph{Security question.}
We will execute AgentDojo injection tasks where the attacker controls child-visible content and attempts:
\begin{enumerate}
  \item direct child egress,
  \item raw-data return through \texttt{submit\_result},
  \item schema-conforming semantic data exfiltration, and
  \item history erasure via abandonment.
\end{enumerate}
We will report attack success rates and the specific structural check that blocked each attempt. Branching is designed to increase utility without weakening sink enforcement; schema-only exits are expected by design to fail against semantic data exfiltration unless supported by a trusted exit assertion~\cite{sabelfeld2009declassification}.

\paragraph{Baselines and ablations.}
We will compare:
\begin{itemize}
  \item an undefended agent;
  \item sink-only IFC that unconditionally accepts acquisition taints;
  \item hard decline of restrictive reads;
  \item Fides's basic and selective-hiding planners~\cite{fides};
  \item APPA with branching disabled; and
  \item full APPA.
\end{itemize}
An abandon-only bounded-exit ablation will isolate the utility of execution isolation from that of the trusted derivative mechanism. We will not evaluate copied or discardable branch logs as functional ablations because they violate the threat model; these configurations will be evaluated strictly as negative invariant test cases.

\paragraph{Main metrics.}
Alongside standard task utility and injection attack success rates, we will measure parent-label descents avoided, post-acquisition benign actions completed, branches created, abandoned, and merged, exit rejections, authority interventions, and token and latency overhead per branch. Both security and utility metrics must be reported, as preventing label creep represents a primary utility benefit under strict enforcement.

\paragraph{Case study.}
We will evaluate the CRM/invoice scenario from \S\ref{sec:branching} end-to-end using the engine implementation and publish its policy contracts, parent/child label traces, shared event log, injected payload, exit assertion, and post-merge benign execution trace.

\paragraph{Secondary study.}
We will annotate $k$ risky tools, classify all remaining tools as \texttt{Unknown}, and perform a parameter sweep over the cast policy space. This study remains secondary to the branch evaluation: incremental tool annotation provides supporting machinery rather than the core contribution.

\paragraph{Non-goals.}
We do not conduct a content-level detection benchmark because APPA does not inspect payload content. Measuring branch latency and token overhead is necessary rather than optional because branch isolation duplicates execution context.

\fixme{The implemented engine lags behind this theoretical model in several respects. Although aligned with the specification regarding binary outcomes and world events modeled as log states with an open declared vocabulary (\texttt{no\_prior}, named per-dispatch waivers), it retains several predecessor characteristics: value-granular per-flow labels rather than per-run labels, state checks evaluated against current rather than committed state, lack of fork-before-dispatch trajectory isolation, absence of checked exit/merge mechanisms, non-atomic render$\rightarrow$rule$\rightarrow$dispatch plan objects, lack of \texttt{prior(k)}, and absence of boundary events. The paper cannot evaluate branching as a taint-prevention mechanism until fork isolation and checked exits are fully implemented. If these features remain absent, the implementation must be framed strictly as a predecessor handling sink enforcement and label propagation, without presenting its AgentDojo metrics as evidence for the primary contribution.}
